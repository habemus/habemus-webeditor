// third-party
const Bluebird = require('bluebird');

module.exports = function (habemus, options) {

  return Bluebird.all([
    require('./menu/project')(habemus, options),
    require('./menu/file')(habemus, options),
    require('./menu/directory')(habemus, options),
  ])
  .then(function (components) {
    
  });
};

// h-dependencies
const HProjectClient = require('habemus-project-client');

module.exports = function (habemus, options) {

  return new HProjectClient({
    serverURI: process.env.H_PROJECT_URI,
  });

};

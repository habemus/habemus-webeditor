// third-party
const Bluebird = require('bluebird');

module.exports = function (habemus, options) {

  return Bluebird.all([
    // require('./header')(habemus, options)
    require('./cloud-notifications')(habemus, options),
  ])
  .then(function (components) {
    // habemus.ui.header = components[0];
    
    
  });
};
